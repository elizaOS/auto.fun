services:
  app:
    build: .
    ports:
      - "${PORT}:${PORT}"
    env_file: 
      - .env
    environment:
      - PORT=${PORT}
      - MONGODB_URI=mongodb://mongo:27017/serlaunchalot

    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: 
